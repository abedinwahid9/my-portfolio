import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["d33wubrfki0l68.cloudfront.net"], // Add the domain of your images
  },
};

export default nextConfig;
